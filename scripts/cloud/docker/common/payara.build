COPY exports/payara.tar.gz /tmp
RUN tar zxf /tmp/payara.tar.gz -C /usr/share && rm -f /tmp/payara.tar.gz \
    && echo "PATH=$PATH:/usr/share/payara/bin" >/etc/profile.d/payara-path.sh \
    && chmod a+x /etc/profile.d/payara-path.sh
